[package]
name = "compositional-diff"
version = "0.1.0"
edition = "2021"
description = "Autodiff and backpropagation via categorical semantics"
license = "MIT OR Apache-2.0"

[dependencies]
compositional-core = { path = "../core" }

[dev-dependencies]
